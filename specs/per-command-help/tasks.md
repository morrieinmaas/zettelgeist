# Tasks

- [ ] 1. Add a `help` string constant to each command module
- [ ] 2. In `bin.ts`, dispatch `--help` to print the command's help and exit 0
- [ ] 3. Keep the global help fallback for unknown commands
- [ ] 4. Add a `--help` test per command in the CLI test suite
- [ ] 5. Update the top-level README's "commands" table to link to the per-command help output
