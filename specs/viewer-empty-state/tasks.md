# Tasks

- [ ] 1. Detect the empty-specs case in the viewer's board component
- [ ] 2. Build an `EmptyState` component with copy + two example invocations
- [ ] 3. Write a unit test that mounts the board with an empty spec list
- [ ] 4. Ensure transition back to the populated board is glitch-free on refresh
- [ ] 5. Update the viewer screenshot in `docs/` if one exists
