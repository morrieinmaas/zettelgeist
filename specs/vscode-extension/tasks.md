# Tasks

- [ ] 1. Scaffold the extension under `packages/vscode-extension` with `yo code` style structure
- [ ] 2. Load the prebuilt viewer bundle into a `WebviewPanel`
- [ ] 3. Implement the `postMessage` backend: list_specs, read_spec, dependencies, statuses
- [ ] 4. Implement mutations via `vscode.workspace.fs` + the VSCode git extension API
- [ ] 5. Add an `e2e` test that opens the webview in `@vscode/test-electron`
- [ ] 6. Document install + publish in `packages/vscode-extension/README.md`
- [ ] 7. Publish to the VSCode Marketplace
