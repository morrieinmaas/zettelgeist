# Tasks

- [ ] 1. Enumerate the final event list and shared fields in `spec/format.md`
- [ ] 2. Author a JSON schema per event type under `spec/events/`
- [ ] 3. Add conformance fixtures that cover every event type
- [ ] 4. Implement an event emitter in `@zettelgeist/core` (async iterable + callback)
- [ ] 5. Wire emission into the status transition + task diff logic
- [ ] 6. Expose the optional events capability on `@zettelgeist/mcp-server`
- [ ] 7. Bump the format version to `0.2` and update the format spec doc
