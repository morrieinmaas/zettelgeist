# Tasks

- [x] 1. Define the event taxonomy (event names, payload shapes, versioning)
- [x] 2. Implement the dispatcher worker with exponential-backoff retry
- [x] 3. Implement HMAC-SHA256 signing of payloads
- [x] 4. Add the endpoint-management UI (list / add / rotate-secret / disable)
- [x] 5. Add the delivery-log UI (recent attempts, response codes, retry button)
- [ ] 6. Write the customer-facing docs at `/docs/webhooks` #human-only
