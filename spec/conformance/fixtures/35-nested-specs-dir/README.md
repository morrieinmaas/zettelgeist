# 35-nested-specs-dir — `specs_dir` can be a deeply nested path

Fixture 10 covers `docs/specs`. This one pushes it further to confirm
the loader resolves arbitrary nested paths, not just one level deep.
