# 28-all-tasks-skipped — every counted task is `#skip` → derived `draft`

`#skip` removes a task from the counted set. If every task carries it,
counted is empty and status falls into the no-tasks branch: `draft`
(since the harness never sets `claimedSpecs`).

Progress: 0/0 because all 3 tasks are excluded from the count.
