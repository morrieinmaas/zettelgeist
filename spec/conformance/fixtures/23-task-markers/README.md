# 23-task-markers — `-`, `*`, and `+` all parse as task lines

The task regex accepts any of the three CommonMark bullet markers. All
three should produce a counted task. Progress: 0/3 (none checked).
