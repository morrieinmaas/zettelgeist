---
status: blocked
blocked_by: >
  waiting on
  external API
  decision
---
# Stuck

YAML folded scalars collapse line breaks into single spaces. The trailing
newline is preserved by gray-matter but stripped by the INDEX renderer's
`.trim()`.
